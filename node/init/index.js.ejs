---
to: src/index.js
overwrite: true
---
const fs = require("fs");
require("js-container")(
    ({ debug }) => debug(),
    ...(fs.readdirSync(__dirname).filter(x => x !== "index.js").map(x => require(`./${x}`)))
);
